{
  "auths": {
    "artifactory-jcr.int.svc.cluster.local:8082": {
      "auth": "{{AUTH_PLACEHOLDER}}"
    },
    "artifactory-jcr.int:8082": {
      "auth": "{{AUTH_PLACEHOLDER}}"
    },
    "artifactory-jcr.int": {
      "auth": "{{AUTH_PLACEHOLDER}}"
    },
    "int-jcr.nguiland.org": {
      "auth": "{{AUTH_PLACEHOLDER}}"
    }
  }
}
