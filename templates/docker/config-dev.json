{
  "auths": {
    "artifactory-jcr.dev.svc.cluster.local:8082": {
      "auth": "{{AUTH_PLACEHOLDER}}"
    },
    "artifactory-jcr.dev:8082": {
      "auth": "{{AUTH_PLACEHOLDER}}"
    },
    "dev-jcr.nguiland.org": {
      "auth": "{{AUTH_PLACEHOLDER}}"
    }
  }
}
