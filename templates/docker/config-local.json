{
  "auths": {
    "artifactory-jcr.local.svc.cluster.local:8082": {
      "auth": "{{AUTH_PLACEHOLDER}}"
    },
    "artifactory-jcr.local:8082": {
      "auth": "{{AUTH_PLACEHOLDER}}"
    },
    "artifactory-jcr.local": {
      "auth": "{{AUTH_PLACEHOLDER}}"
    }
  }
}
